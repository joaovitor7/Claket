def avaliar():
    # ler json
    # cria lista de Tags
    # laço de i em lista:
        # se não tem sentimento
            # sentimento, qtTweets = determinarSentimento(texto da tag)
            # seta em i os valores recebidos

    ######### todas as palavras com sentimento
    # nota = calculaNota(lista)
    # cria um novo json

    return #o novo json

def determinarSentimento(tag):
    # coleta tweets
    # conta quantos tweets foram coletados
    # classifica os tweets
    # verifica qual classe predominou

    return #sentimento, qtTweets

def calculaNota(listaTags):

    return #nota

def reavaliar():
    return